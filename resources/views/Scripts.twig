<script type="text/javascript">
    window.fastCheckOutPublicPath = "{{ plugin_path('FastCheckout') ~ '/js/dist/' }}"
</script>

{% if services.template.isCheckout() == "1" or services.template.getCurrentTemplate() == 'tpl.confirmation' %}
    {% set minSuffix = '.min' %}
    {% if ceresConfig.log.performanceLevel  == 'development' %}
        {% set minSuffix = '' %}
    {% endif %}

<!-- SSR:entry({{ ssr_entry('FastCheckout', 'resources/js/dist/fastCheckout-server' ~ minSuffix ~ '.js') }}) -->
    <script src="{{ plugin_path('FastCheckout') }}/js/dist/fastCheckout{{ minSuffix }}.js?v={{ buildHash }}"></script>
    <script src="{{ plugin_path('FastCheckout') }}/js/dist/fastCheckout-base{{ minSuffix }}.js?v={{ buildHash }}"></script>
{% endif %},
